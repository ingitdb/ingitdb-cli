# 📁 `.ingitdb/` — inGitDB Configuration Directory

This directory is the inGitDB configuration root for this repository.
It tells inGitDB which collections exist and how they are organised.

## ⚙️ Config files

| File                   | Required | Purpose                                                      |
| ---------------------- | -------- | ------------------------------------------------------------ |
| `root-collections.yaml`| optional | Maps collection IDs → directory paths (including namespace imports) |
| `settings.yaml`        | optional | Repository-level settings: `default_namespace`, `languages` |

Both files are optional — an empty repository with neither file is a valid (empty) inGitDB instance.

See the full schema reference: [`docs/schema/root-config.md`](../docs/schema/root-config.md)

---

## 📖 Overview

> _Describe what this inGitDB instance stores._
> _Replace this placeholder with a short summary of your data model._

---

## 📊 Stats

> _This section is intended to be auto-generated by inGitDB tooling._

| Metric           | Value |
| ---------------- | ----- |
| Collections      | —     |
| Total records    | —     |
