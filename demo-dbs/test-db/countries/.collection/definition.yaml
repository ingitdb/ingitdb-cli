titles:
  en: Countries
record_file:
  name: "{key}/{key}.yaml"
  type: "map[string]any"
  format: yaml
columns:
  titles:
    type: "map[locale]string"
    required: true
  population:
    type: number
  area_km2:
    type: number
  currency:
    type: string
  flag:
    type: string
columns_order:
  - flag
  - titles
  - population
  - area_km2
  - currency
default_view: {}
