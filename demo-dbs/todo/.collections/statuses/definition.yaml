titles:
  en: Statuses
  ru: Статусы
record_file:
  name: "statuses.yaml"
  type: "map[$record_id]map[$field_name]any"
  format: yaml
columns:
  titles:
    type: "map[locale]string"
    required: true
